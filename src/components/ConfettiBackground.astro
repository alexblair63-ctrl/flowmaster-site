---
// Subtle ambient confetti background - CSS-only implementation
---

<div class="confetti-background" aria-hidden="true"></div>

<style>
  .confetti-background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 0;
    overflow: hidden;
  }

  /* Create floating particles using pseudo-elements */
  .confetti-background::before,
  .confetti-background::after {
    content: '';
    position: absolute;
    width: 100%;
    height: 100%;
    background-image:
      /* Red particles */
      radial-gradient(circle at 20% 30%, rgba(244, 63, 67, 0.12) 0%, transparent 2px),
      radial-gradient(circle at 60% 70%, rgba(244, 63, 67, 0.1) 0%, transparent 3px),
      radial-gradient(circle at 80% 20%, rgba(244, 63, 67, 0.08) 0%, transparent 2px),
      radial-gradient(circle at 40% 80%, rgba(244, 63, 67, 0.15) 0%, transparent 2px),
      /* Green particles */
      radial-gradient(circle at 30% 50%, rgba(107, 229, 83, 0.1) 0%, transparent 2px),
      radial-gradient(circle at 70% 40%, rgba(107, 229, 83, 0.12) 0%, transparent 3px),
      radial-gradient(circle at 50% 90%, rgba(107, 229, 83, 0.08) 0%, transparent 2px),
      radial-gradient(circle at 90% 60%, rgba(107, 229, 83, 0.1) 0%, transparent 2px);
    background-size: 100% 100%;
    animation: float-up 40s linear infinite;
  }

  .confetti-background::after {
    animation-duration: 50s;
    animation-delay: -10s;
    opacity: 0.7;
  }

  @keyframes float-up {
    0% {
      transform: translateY(0) translateX(0);
    }
    50% {
      transform: translateY(-50vh) translateX(20px);
    }
    100% {
      transform: translateY(-100vh) translateX(0);
    }
  }

  /* Respect user's motion preferences */
  @media (prefers-reduced-motion: reduce) {
    .confetti-background::before,
    .confetti-background::after {
      animation: none;
      opacity: 0.05;
    }
  }
</style>
